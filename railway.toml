# DEFAULT = frontend (tranquil-comfort / roguepiece.freakydev.com).
# API service must set Config-as-code path to /railway.api.toml

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.web"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
