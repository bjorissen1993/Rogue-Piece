services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: roguepiece
    ports:
      - "5433:5432"
    volumes:
      - roguepiece_pg:/var/lib/postgresql/data

volumes:
  roguepiece_pg:
