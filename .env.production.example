# Production frontend build (Hostnet static upload)
# Copy to .env.production before `npm run build:web`, or export these in the shell.

VITE_API_URL=https://api.roguepiece.freakydev.com

# Your Google email(s) — Sandbox only visible when signed in as these (production builds)
VITE_DEV_ACCESS_EMAILS=
